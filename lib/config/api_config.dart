class ApiConfig {
  // =========================================================
  // CHANGE ONLY THIS URL WHEN YOUR BACKEND DOMAIN CHANGES
  // =========================================================
  static const String baseUrl = "http://10.51.231.80:8080";

  // =========================================================
  // AUTH
  // =========================================================
  static const String login = "/auth/login";
  static const String logout = "/auth/logout";

  // =========================================================
  // PARENT
  // =========================================================
  static const String parents = "/api/v1/parents";

  // =========================================================
  // STUDENTS
  // =========================================================
  static const String students = "/api/v1/students";

  // =========================================================
  // ATTENDANCE
  // =========================================================
  static const String attendance = "/api/v1/attendance";

  // =========================================================
  // HOMEWORK
  // =========================================================
  static const String homework = "/api/v1/homework";

  // =========================================================
  // TIMETABLE
  // =========================================================
  static const String timetable = "/api/v1/timetable";

  // =========================================================
  // RESULTS
  // =========================================================
  static const String results = "/api/v1/results";

  // =========================================================
  // FEES
  // =========================================================
  static const String fees = "/api/v1/fees";

  // =========================================================
  // NOTICES
  // =========================================================
  static const String notices = "/api/v1/notices";

  // =========================================================
  // NOTIFICATIONS
  // =========================================================
  static const String notifications = "/api/v1/notifications";

  // =========================================================
  // BIRTHDAY STATUS
  // =========================================================
  static const String birthdayStatus =
      "/api/v1/student-birthday-status";

  // =========================================================
  // BIRTHDAY CHAT
  // =========================================================
  static const String birthdayChat =
      "/api/v1/birthday-chat";

  // =========================================================
  // DAILY TEST / MCQ
  // =========================================================
  static const String dailyTest = "/api/v1/daily-test";
}